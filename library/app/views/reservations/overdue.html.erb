<h1>Overdue Books</h1>
  <ol id="reservations">
  <% @reservations.each do |reservation| %>
    <h4><class="reservation"><%= pluralize(reservation.overdue, 'overdue') %> </class></h4>
    <li>
    <!--<li> -->
      <div class="res-bg">
      <br>
      <p> <%= image_tag reservation.book.image_cover_url, class:"cover_image" %></p><br>
      <p><h4><%= link_to(reservation.book.title, reservation.book) %></h4></p>


