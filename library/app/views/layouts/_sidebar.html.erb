<div id="sidebar">
    <!-- check if user is logged in -->
    <% if user_is_logged_in? %>
        <div class="panel panel-primary">
          <div class="panel-heading">
            <h3 class="panel-title">Navigation</h3>
          </div>
          <div class="panel-body">
            <p>
              <%= link_to 'All Books', books_path %>
            </p>
            <p> <%= link_to 'Authors', authors_path %> </p>
            <p>My Reservations
              <%# <%= link_to 'My Reservations', reservations_path %>
            </p>

            <% if show_links == true %>
                <p> <%= link_to 'New Book', new_book_path %></p>
                <p><%= link_to 'Overdue Books', books_path %> </p>
                <p><%#<%= link_to 'Log out', session_path(:user_id), method: :delete%></p>
            <% end -%>
            <p><%= button_to 'Logout', logout_path, method: :delete, class:"btn btn-primary" %></p>
          </div>
        </div>
    <% end %>
</div>
